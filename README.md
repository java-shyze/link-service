# LinkService

## Usage .env
To initialize the .env file, import the appropriate package: `import com.links.linkservice.config.EnvConfig;`
And use `EnvConfig.loadEnv();` in a static block or function at the beginning.